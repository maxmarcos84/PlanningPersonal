@model IEnumerable<PlanningPersonal.DTOs.EmployeeDto>
@{
}
<div class="container-fluid">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Employee</h1>
        <a asp-action="Create" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">
            <i class="fas fa-plus fa-sm text-white-50"></i> New Employee
        </a>
    </div>
    <div class="row">
        @if (Model.Count() > 0)
        {
            @foreach (var item in Model)
            {
                <div class="col-xl-3 col-md-6 mb-4">
                    <div class="card border-left-primary shadow h-100 py-2">
                        <div class="card-body">
                            <div class="row no-gutters align-items-center">
                                <div class="col mr-2">
                                    <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">
                                        @item.LastName @item.Name
                                    </div>
                                </div>
                                <div class="col mr-2">
                                    <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">
                                        @item.EmployeeNumber
                                    </div>
                                </div>
                                <div class="col mr-2">
                                    <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">
                                        @item.Company.Name
                                    </div>
                                </div>
                                <div class="col-auto">
                                    <a type="button" class="btn btn-sm" asp-controller="Employee" asp-action="" asp-route-id="@item.Id"
                               onclick="return confirm('Are you sure you wants to delete?');">
                                        <i class="fas fa-trash text-gray-300"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <h1>No Employees to display</h1>
        }

    </div>
</div>


